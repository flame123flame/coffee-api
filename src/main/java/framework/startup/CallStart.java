package framework.startup;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import coffee.lottoconfig.service.CloseNumberService;

// @Configuration
public class CallStart {
    // @Autowired
    // private CloseNumberService closeNumberService;

    // @Bean
    public void callStartCloseNumber() {
        // closeNumberService.updateCloseNumberCache();
    }

}